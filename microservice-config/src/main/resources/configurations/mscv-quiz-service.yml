server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_quizapp
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

springdoc:
  swagger-ui:
    path: /swagger-ui.html